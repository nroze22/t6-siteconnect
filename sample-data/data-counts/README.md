# Synthetic Data COUNTS fixtures

Generated by `npm run demo:fixtures` from the same engine used by the rehearsal. No real patients or hospital data. Source v1 deliberately contains a duplicate Observation and a missing unit. Source v2 supplies the correction. Permission fixtures are separate from source demographics. Expected outputs use the custom `talosix-demo-labs/3` schema and illustrative privacy tokens/date offsets, not approved PPRL or de-identification.

See `DATA-COUNTS-DEMO.md` at the repository root for the walkthrough, counts and limitations.

Current request: `request-v3.json`. Earlier request versions are historical. Source v3 adds lifecycle examples; expected-lifecycle-refresh.json contains the corresponding output.
